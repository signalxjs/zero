/*
 * Types for `import '@sigx/zero/css'` — the extensionless export has no
 * `.css` extension for a bundler's `*.css` ambient module to match, and
 * TypeScript 6 checks side-effect imports by default. A stylesheet has no
 * bindings, so the declaration is an empty module.
 */
export {};
